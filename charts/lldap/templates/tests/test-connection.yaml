apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "boilerplate.fullname" . }}-test-connection"
  labels:
    {{- include "boilerplate.labels.standard" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "boilerplate.fullname" . }}:{{ .Values.service.webui.port }}']
  restartPolicy: Never
